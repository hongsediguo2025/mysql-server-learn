accepted_epoch_delta_before_final_ack
0
ERROR HY000: DRAIN TRANSACTIONS PRESERVE was reset before shutdown handoff
SELECT GROUP_CONCAT(CONCAT(id, ':', v) ORDER BY id) AS committed_values
FROM t_closing_early_pipeline;
committed_values
1:121,2:241
